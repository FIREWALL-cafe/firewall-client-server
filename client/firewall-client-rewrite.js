

// declare global variables

storage.get([
  'clientId',
  'pendingQuery', 
  'autocompleteEnabled'
], init)

function init(storage) {
  console.log("got storage object:", storage)
  // set up UI

  // create event listeners

  // begin main() loop
}

